# GitHub Pages 正式路径；本地根路径或以后自有域名使用 /
VITE_APP_BASE=/birthday-card/
# 空值读取本站 media/；自有域名可填写可信 HTTPS 资源根并以 / 结尾。
# 没有自有域名时使用独立 Worker 媒体网关的 HTTPS 根，不使用 r2.dev 上线。
VITE_ASSET_BASE_URL=
VITE_CONTENT_MODE=demo
# 私密凭证只放 Actions Secrets 或本机会话，不能使用 VITE_ 前缀。
